.toolbar
  %ul
    %li
      Name 
      = link_to "U", customers_path(:order => "name asc")
      = link_to "D", customers_path(:order => "name desc")
    %li
      Shortname
      = link_to "U", customers_path(:order => "shortname asc")
      = link_to "D", customers_path(:order => "shortname desc")
    %li Locations
-@customers.each do |customer|
  .customer
    - if customer.id == @customer.id && "#{params[:controller]}/#{params[:action]}" != 'customers/edit' 
      %ul.titleActive
        %li= customer.name
        %li= customer.shortname
        %li= customer.locations.count
        %li.actions
          = link_to 'C', customers_path(:order => params[:order])
          = link_to 'E', edit_customer_path(customer, :order => params[:order])
          = link_to 'D', customer_path(customer, :order => params[:order]), :confirm => 'Are you sure?', :method => :delete
        .clear
      .nestedRessources
        = render :partial => 'customers/details/locations'
        = render :partial => 'customers/details/contacts'
        .clear
    - else
      %ul.titleInactive
        %li= customer.name
        %li= customer.shortname
        %li= customer.locations.count
        %li.actions
          = link_to 'O', customer_path(customer, :order => params[:order])
          = link_to 'E', edit_customer_path(customer, :order => params[:order])
          = link_to 'D', customer_path(customer, :order => params[:order]), :confirm => 'Are you sure?', :method => :delete
        .clear
      .clear